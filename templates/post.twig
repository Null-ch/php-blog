{% extends 'base.twig' %}

{% block body %}
    <div class="container">
        <img src="img/Жулик.png" class="img-fluid mt-3 mb-3 "  alt="..." width="400" height="800">
        <h1 class="mt-3 mb-3">{{ post.title|escape }}</h1>
        <div class="container mb-3">
            <div class="row">
                <span class="text-muted">Max Pronko | {{ post.published_date|date("F d, Y") }}</span>
            </div>
        </div>
        <div>
            {{ post.content|raw }}
        </div>
    </div>
{% endblock %}